## 1. Add short flags to CLI decorators

- [x] 1.1 Add short flags to the `build` command's `@click.option` decorators in `src/cartoload/cli.py`
- [x] 1.2 Add short flags to the `download` command's `@click.option` decorators
- [x] 1.3 Add short flags to the `list` command's `@click.option` decorators
- [x] 1.4 Add short flags to the `split` command's `@click.option` decorators

## 2. Verify

- [x] 2.1 Run `just check` and `just check types` to verify formatting and type correctness
- [x] 2.2 Run `just test` to verify all tests pass
